# VCS & CI
.git/
.github/

# OS/editor junk
.DS_Store
Thumbs.db

# Build/temp
node_modules/
vendor/
*.log
*.zip

# Local-only folders (examples — adjust as needed)
Backups/
Plans and reports/
Ref Plugins/
WPCode snippets/

# Dev-only validation assets (never needed at runtime)
tests/
